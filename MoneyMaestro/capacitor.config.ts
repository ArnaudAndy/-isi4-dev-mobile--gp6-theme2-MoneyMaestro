import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.moneymaestro.starter',
  appName: 'Money Maestro',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
